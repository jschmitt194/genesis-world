Genesis = Genesis or {}

Genesis.config = {
    meters_per_block = 1,
    sea_level = 0,

    region_size = 16,

    autosave_ticks = 60,

    life_tick_interval = 5,

    hunger_threshold = 70,
    thirst_threshold = 70,

    hunger_rate = 0.1,
    thirst_rate = 0.15,
    energy_rate = 0.05,
    stamina_recovery_rate = 0.1,

    drink_amount = 10,
    drink_thirst_restore = 25,

    eat_amount = 5,
    eat_hunger_restore = 20,

    base_vision = 12,
    elevation_vision_step = 10,
    elevation_vision_bonus = 1,
    max_elevation_vision_bonus = 50,

    fire_damage_radius = 0.5,
    fire_safe_warm_radius = 1.0,
    fire_heat_radius = 3.0
}
